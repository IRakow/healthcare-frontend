steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'build',
      '-t', 'gcr.io/$PROJECT_ID/healthcare-frontend',
      '.'
    ]
images:
  - 'gcr.io/$PROJECT_ID/healthcare-frontend'

# Optional automatic deploy to Cloud Run after build:
# Uncomment below if you want CI/CD
# substitutions:
#   _REGION: us-central1
#   _SERVICE: healthcare-frontend
#   _PROJECT_ID: your-gcp-project-id
# 
# steps:
#   - name: gcr.io/cloud-builders/gcloud
#     args:
#       - run
#       - deploy
#       - $_SERVICE
#       - --image=gcr.io/$PROJECT_ID/healthcare-frontend
#       - --region=$_REGION
#       - --platform=managed
#       - --allow-unauthenticated